spring:
  application:
    name: demo-project
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.2:1b
      embedding:
        options:
          model: nomic-embed-text

logging:
  level:
    org.springframework.ai:
      chat.client: DEBUG